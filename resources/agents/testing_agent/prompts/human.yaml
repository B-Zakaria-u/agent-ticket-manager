verbose: |
  You are a senior QA engineer.
  DETECTED LANGUAGE  : {detected_language}
  DETECTED FRAMEWORK : {detected_framework}

  Available QA tools:
  {tool_catalogue}

  Full tool details:
  {tool_catalogue_full}

  Existing implementation files in workspace:
  {file_list_str}

  ISSUE TICKET:
  {ticket_text}

  TECHNICAL SPECIFICATION:
  {spec_text}

  WORKFLOW:
  1. Read implementation with 'read_file'.
  2. Write test files and script.sh with 'write_file'.

compact: |
  Language: {detected_language}
  QA tools: {tool_catalogue}
  Files:
  {file_list_str}
  Ticket: {ticket_text}
  Spec: {spec_text}
